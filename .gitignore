# Dependency directories
/node_modules

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm
*.tgz
.yarn-integrity
.pnpm-store

# Editor directories and files
.idea
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.vspscc
.history/
.DS_Store
Thumbs.db

# Build artifacts
/dist
/build
/out
/lib # Common output directory for compiled TS
/es # Another common output directory for ES modules

# TypeScript build info
*.tsbuildinfo

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Coverage directory
/coverage

# Test artifacts
/.nyc_output

# Production artifacts
/package-lock.json # Keep if using npm, ignore if strictly Yarn/pnpm
/yarn.lock # Keep if using Yarn, ignore if strictly npm/pnpm
/pnpm-lock.yaml # Keep if using pnpm, ignore if strictly npm/Yarn

# Temporary files
*.tmp
*.log
*.swp
.eslintcache

# macOS specific
.DS_Store

# Windows specific
ehthumbs.db
Thumbs.db

# VS Code local history (if not in .vscode/)
.history/